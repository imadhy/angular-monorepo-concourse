---
platform: linux

image_resource:
  type: registry-image
  source: { repository: node, tag: '10' }

inputs:
  - name: mars-monorepo

run:
  path: /bin/sh
  args: ['angular-monorepo-concourse/ci/test.sh']
