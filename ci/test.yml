---
platform: linux

image_resource:
  # type: docker-image
  # source: { repository: teracy/angular-cli, tag: '8.2.2' }
  type: registry-image
  source: { repository: node, tag: '10' }

inputs:
  - name: angular-monorepo-concourse

run:
  path: angular-monorepo-concourse/ci/test.sh
